# RUNBOOK — 백업·복구 절차 (이음)

작성 2026-09-05. 대상: 운영 담당자(코디네이터 관리자) · 개발 담당자.
이 문서는 **데이터를 잃었을 때 되돌리는 절차**와 **평상시 백업 절차**를 정의한다.

> 원칙: 복원은 운영 데이터를 덮어쓰는 되돌리기 어려운 작업이다.
> 화면에는 백업 생성·무결성 확인까지만 두고, **실제 복원은 이 문서 절차 + 승인**으로만 수행한다.

---

## 0. 현재 데이터가 어디 있는가

| 저장소 | 내용 | 비고 |
| --- | --- | --- |
| 브라우저 localStorage (`eum:appdata:v1`) | 운영 중 상태 전체 | 기본 경로. 기기·브라우저 단위로 분리됨 |
| Supabase (연결 시) | 동일 스키마의 실 테이블 | `VITE_SUPABASE_URL`·`ANON_KEY` 설정 시에만 사용 |
| 백업 파일 (`eum-backup-*.json`) | 스냅샷 1개 = 전체 상태 | 이 문서의 대상 |

`src/eum/storage.js` 의 읽기 순서는 **Supabase → localStorage → 시드**다.
따라서 localStorage 가 비면 화면은 시드로 되돌아가며, **운영 데이터는 백업 파일에서만 복구된다.**

백업 대상 컬렉션(9종, `BACKUP_TABLES`):
`participants` · `applications` · `verifications` · `matches` · `activities` · `activity_logs` · `settlements` · `safety_incidents` · `surveys`
(선택: `notices` · `consents`)

---

## 1. 평상시 백업 (운영자)

1. 코디네이터 화면 → **감사 로그** → 하단 **데이터 백업·무결성 확인**
2. **백업 파일 만들기** 클릭 → `eum-backup-YYYYMMDD-HHMM.json` 저장
3. 저장 위치: 조직이 지정한 **잠금 보관 폴더**(개인 PC 바탕화면 금지)
4. 다음 날 또는 다음 접속 시 **백업 파일 무결성 확인** 으로 1회 검증

**주기(권장, 조직 확정 필요)**

| 구분 | 주기 | 보관 |
| --- | --- | --- |
| 정기 백업 | 주 1회 (매주 월요일 업무 시작 전) | 최근 8주분 |
| 월말 백업 | 매월 말일 | 12개월 |
| 변경 전 백업 | 정산 확정·대량 매칭·데이터 정리 **직전** | 해당 작업 종료까지 |

**중요**
- 백업 파일 원본에는 **전화번호·주소·비상연락처 등 개인정보가 포함**된다. 암호화 저장 또는 잠금 보관이 필수이며, 메신저·메일로 그대로 보내지 않는다.
- 공유·점검용으로 내보낼 때는 **반출용 마스킹본** 버튼을 쓴다. 마스킹본은 설계상 **복원에 사용할 수 없다**(`redacted: true` → 검증 실패).
- 백업 파일은 브라우저에서만 생성되며 외부로 전송되지 않는다. **원격 백업 저장소로의 자동 업로드는 [승인 필요]** 트랙이다.

---

## 2. 백업 파일 형식

```json
{
  "format": "eum.backup",
  "version": 1,
  "created_at": "2026-09-05T09:30:00.000Z",
  "source": "coordinator",
  "redacted": false,
  "counts": { "participants": 27, "activities": 38, "...": 0 },
  "checksum": "4b4c5fad856e2e05",
  "tables": { "participants": [], "...": [] }
}
```

- `checksum` 은 테이블 내용을 **키 순서와 무관하게** 해싱한 값(`checksumOf`)이다.
  파일이 한 글자라도 바뀌면 검증에서 걸린다.
- `counts` 는 실제 배열 길이와 대조된다(부분 손실 탐지).
- `version` 이 앱보다 높으면 복원하지 않는다(앱을 먼저 올린다).

---

## 3. 장애 유형별 대응

### 3-1. 화면 데이터가 시드로 되돌아갔다 (localStorage 유실)

증상: 참여자·활동이 데모 값으로 보인다.

1. **그 브라우저에서 아무 작업도 하지 말 것** — 새 저장이 일어나면 덮어쓴다.
2. 다른 기기/브라우저에 같은 데이터가 살아 있는지 먼저 확인한다.
3. 가장 최근 백업 파일을 찾아 **무결성 확인**(2단계 UI)을 통과시킨다.
4. 아래 **4. 복원 절차**를 수행한다. (승인 필요)

### 3-2. 백업 파일이 손상되었다

- 무결성 확인에서 `체크섬 불일치` 또는 `건수 불일치`가 나오면 **그 파일로 복원하지 않는다.**
- 한 세대 이전 백업으로 내려가 같은 검증을 반복한다.
- 손상 파일은 삭제하지 말고 별도 폴더에 보관한다(원인 분석용).

### 3-3. 잘못된 대량 변경(오조작)

- 변경 전 백업이 있으면 3-1 과 동일하게 복원한다.
- 없으면 **감사 로그**(코디네이터 → 감사 로그)로 누가·언제·무엇을 바꿨는지 확인해 수기 복구 범위를 산정한다.
  단, 감사 로그는 현재 **세션 메모리 한정**이다(서버 영구 보관은 [승인 필요]).

### 3-4. Supabase 연결 환경

- 실 DB를 쓰는 환경에서는 **Supabase 자체 백업(PITR)** 이 1차 수단이다. 콘솔 복구는 되돌리기 어려운 작업이라 **개발 담당자 + 승인** 하에만 수행한다.
- 이 문서의 파일 백업은 Supabase 백업을 **대체하지 않는다**. 이중화 수단으로 병행한다.

---

## 4. 복원 절차 (승인 필요)

> 실행 전 확인: ① 승인권자 승인 ② 현재 상태의 백업을 먼저 1개 생성 ③ 대상 파일 무결성 확인 통과

1. **현재 상태 백업** — 감사 로그 화면에서 백업 파일을 하나 만든다(복원이 잘못될 때 되돌아올 지점).
2. **대상 파일 검증** — 같은 화면의 *백업 파일 무결성 확인* 으로 "복원 가능한 백업입니다"를 확인한다.
3. **복원 실행** — 개발 담당자가 `src/eum/backup.js` 의 `restoreState()` 결과를 `eum:appdata:v1` 에 기록한다.

   ```js
   // 복원 대상 브라우저의 개발자도구 콘솔 — 2단계(무결성 확인)를 통과한 파일에만 사용한다.
   const snap = JSON.parse(백업파일_내용_문자열);   // 파일 내용을 그대로 붙여넣는다
   if (snap.format !== 'eum.backup' || snap.redacted) throw new Error('복원 불가 파일');
   localStorage.setItem('eum:appdata:v1', JSON.stringify(snap.tables));
   location.reload();
   ```

   `storage.loadState()` 는 필수 9개 컬렉션이 배열인지 다시 확인하므로, 형태가 깨진 값은 반영되지 않고 시드로 폴백한다.
4. **복원 후 대조** — 참여자/활동/정산 건수를 백업 파일 `counts` 와 눈으로 대조한다.
5. **기록** — 아래 6장 이력표에 일시·사유·대상 파일·결과를 남긴다.

**복원하지 말아야 하는 파일**
- `redacted: true` (마스킹본)
- 무결성 확인 실패 파일
- `version` 이 앱보다 높은 파일

---

## 5. 복구 리허설

절차가 실제로 동작하는지 **분기 1회** 확인한다. 리허설은 코드로 자동화되어 있다.

```bash
npm run rehearse:restore
```

수행 단계: 스냅샷 생성 → 파일 직렬화 → 파싱 → 무결성 검증 → 복원 → **원본 대조** → 정규화 적용 → **변조 감지**.
전 단계 통과 시 종료코드 0, 한 단계라도 실패하면 1 (CI 게이트로 사용 가능).

### 리허설 기록

#### 2026-09-05 · 초회 리허설 (자동)

`[PASS] 2026-09-05T03:44:57 · 레코드 155건 · 체크섬 4b4c5fad856e2e05`

| 단계 | 결과 | 비고 |
| --- | --- | --- |
| 스냅샷 생성 | PASS | 9개 필수 컬렉션 |
| 파일 직렬화 | PASS | 56,468 bytes |
| 파일 파싱 | PASS | OK |
| 무결성 검증 | PASS | 체크섬 4b4c5fad856e2e05 |
| 복원 | PASS | 완료 |
| 원본 대조 | PASS | 전 컬렉션 원본 일치 |
| 정규화 적용 | PASS | 화면 상태 구성 가능 |
| 변조 감지 | PASS | 체크섬으로 차단됨 |

- 대상 데이터: `SEED_DATA` 기준 155건
  (`participants` 27 · `applications` 9 · `verifications` 13 · `matches` 5 · `activities` 38 · `activity_logs` 33 · `settlements` 16 · `safety_incidents` 3 · `surveys` 9 / 선택 `notices` 2)
- 실행 환경: node v22.23.2
- 회귀 고정: `tests/backup.test.mjs` 14건 (전체 176건 통과)
- 한계: 이번 리허설은 **파일 왕복·무결성·복원 정합**까지를 검증한다. 실제 운영 브라우저에서의 localStorage 덮어쓰기(4장 3단계)와 Supabase 콘솔 복구는 **승인 후 수기 리허설** 대상이다.

#### 다음 리허설 예정

| 일자 | 담당 | 범위 |
| --- | --- | --- |
| 2026-12 (분기) | 개발 담당자 | `npm run rehearse:restore` + 실제 브라우저 복원 1회(승인 시) |

---

## 6. 복원 이력

실제 복원을 수행할 때마다 한 줄씩 남긴다.

| 일시 | 사유 | 대상 백업 파일 | 승인자 | 수행자 | 결과 |
| --- | --- | --- | --- | --- | --- |
| (없음) | | | | | |

---

## 7. 아직 열지 않은 것 (승인 필요)

- **원격 백업 저장소 자동 업로드** — 현재 백업은 담당자 기기에 저장된다. 오프사이트 보관은 계약·보안 검토 후.
- **자동 백업 스케줄** — 주기 실행은 서버 또는 확장 기능이 필요하다.
- **화면에서의 복원 버튼** — 오조작 위험이 커서 의도적으로 두지 않았다. 도입 시 2단계 확인·감사 기록·직전 자동 백업이 함께 필요하다.
- **감사 로그 서버 영구 보관** — 현재 세션 메모리 한정.

---

## 관련 파일

- `src/eum/backup.js` — 스냅샷 생성·검증·복원·마스킹·리허설 로직
- `src/eum/apps/BackupPanel.jsx` — 운영자 화면(백업 생성·무결성 확인)
- `scripts/rehearse-restore.mjs` — 리허설 실행기
- `tests/backup.test.mjs` — 회귀 고정
- `src/eum/storage.js` — 저장·로드 경로(변경 없음)
