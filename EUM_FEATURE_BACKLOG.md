# 이음 기능 백로그 — 운영 완성도(사업계획 보강용) · 야간 우선

작성 2026-07-31. 출처: 외부 검토에서 "새로 반영하면 이득"인 운영 디테일만 발췌. 
원칙: **기존 리듀서/SEED/코디네이터 로직은 유지하고 추가(additive)만**. 데모 SEED 범위 내 기능이라 저·중위험(개인정보 실수집·결제·RLS 아님) → 야간 자율 개발·배포 가능.

## 반영할 것 (우선순위)
### 1. 정산 이의신청 처리 (신뢰성·완결성)
- 참여자(청년·어르신)가 **정산 내역**에서 "이의 신청"(사유 입력) → 상태 '이의접수'.
- **코디네이터**가 이의 목록에서 검토 → 승인/반려 + 메모, 처리 이력 기록.
- 추가: SEED 정산 항목에 dispute 필드+샘플 1~2건, reducer 액션(예: RAISE_SETTLEMENT_DISPUTE / RESOLVE_SETTLEMENT_DISPUTE) **추가**, 참여자 정산 화면 버튼+모달, 코디 정산 화면 이의 탭/배지.

### 2. 채널별 공지 발송 + 전달 결과 확인(미전달 재발송)
- 공지 작성 시 **채널 선택(카톡·문자·앱)**, 발송 후 **수신자별 전달상태(전달/미전달)** 표시.
- **미전달만 재발송** 버튼. (실제 발송은 데모/시뮬레이션 — 실채널 연동 아님)
- 추가: SEED 공지에 channels·delivery 결과, reducer(SEND_NOTICE / RESEND_UNDELIVERED) 추가, 코디 공지 화면 UI.

### 3. 실적 보고서 월/분기 집계·다운로드 + 참여자 통합 검색
- 실적/리포트 화면에 **월·분기 토글 집계**(순수 집계 함수), **CSV 다운로드**(클라이언트 생성, Blob).
- **참여자 통합 검색/조회**(이름·역할·지역 필터).
- 추가: 집계 유틸(순수), CSV export 유틸, 검색 UI. 개인정보 신규 수집 없음(기존 SEED 조회).

## 산출물(코드 아님, 선택)
- 기능 3단 위계(요구사항→기능→상세기능) 명세서 — 사업계획서 첨부/2라운드용. 요청 시 별도 문서로 생성.

## 완료 로그
- 2026-08-20(야간) **활동기록 검증 루프 완성 — 보완 요청(되돌려 보내기)**: 코디 '활동 승인' 화면에 승인 버튼만 있어, 승인하지 않은 기록은 사유 없이 '승인 대기'에 남아 있었음(작성자는 왜 승인이 안 났는지 알 수 없고 코디도 검토 이력이 남지 않음). 정산 이의 루프와 동일 문법으로 반대편을 추가 — ① 리듀서 `REJECT_LOG` **추가**(APPROVE_LOG·approved 필드 무변경, `revision:{status,note,requested_at,requested_by}`만 덧붙임) ② 코디: 행 단위 '보완 요청' 버튼 + 사유 모달(인라인 오류·연타 방지 throttle), '보완 요청' 탭(대기 목록에서 분리)과 목록 내 사유·요청일·요청자 표시, 이미 요청한 건은 '사유 수정', 승인 버튼은 그대로 유지(보완 후 바로 승인 가능) ③ 청년 앱 활동 기록: '보완 요청' 배지·탭 + 사유 배너(role=status, 재방문해도 확인 가능) ④ validate.js `validateRevisionNote` 추가(500자·처리 메모와 동일 상한), audit.js `REJECT_LOG` 감사 대상 추가(사유 자유 텍스트는 미기록, 식별자·상태값만) ⑤ SEED log108에 데모 샘플 1건. 일괄 승인·정산 반영 로직 무변경. esbuild EXIT 0 · node --test 65/65 · 널바이트 0 · 파일 완전성 확인.
- 2026-08-18(야간) **폼 오류 인라인 표시 — 입력검증 결과를 화면에 남김**: 2026-08-14에 표준화한 validate.js 결과가 전부 토스트로만 떠서, 사라진 뒤에는 어느 칸이 왜 잘못됐는지 확인할 수 없었음(ui.jsx의 Field `error/errorId`·Input/Textarea `error/describedBy` 인프라는 이미 있었으나 미사용). 4개 폼에 인라인 오류 연결 — ① 청년 정산 이의 사유 ② 어르신 정산 문의(전용 대활자 16px 배너, aria-invalid·빨강 테두리) ③ 코디 이의 처리 메모 ④ 코디 공지 제목·내용(어느 칸인지 구분). validate.js에 `validateNoticeFields`(field:'title'|'body' 동반 반환) **추가**(기존 validateNotice 유지). 입력 시작 시 오류 자동 해제, 모달 닫기·재열기 시 초기화. 필드성 오류만 인라인으로 옮기고 연타 방지·채널 미선택·대상 없음 등 폼 전역 오류는 기존 토스트 유지. 리듀서/SEED/발송 로직 무변경. esbuild EXIT 0 · node --test 63/63 · 널바이트 0 · 파일 완전성 확인.
- 2026-08-18(주간) **빈 상태 회복 행동(CTA) — 디자인시스템 §5 준수**: 빈 상태 26곳 중 행동 버튼이 있는 곳이 1곳뿐이라 "아이콘+안내+행동 버튼" 규칙 미충족. 원인이 **필터·탭**이라 회복이 순수 로컬 setState인 4곳에만 CTA 추가 — ① 코디 신청자 관리(검색·타입필터 활성 시 '검색·필터 초기화'), ② 코디 참여자 통합 검색(검색어·역할·지역 중 하나라도 걸려 있을 때 초기화), ③ 감사 로그(분류 필터 시 제목·설명 분기 + '전체 기록 보기'), ④ 청년 활동 기록(전체 탭이면 '새 기록 작성', 승인/대기 탭이면 '전체 기록 보기' + 빈 sub 카피 보강). 네비게이션이 필요한 진짜 빈 상태(정산 없음·매칭 전 등)는 손대지 않음. 순수 표현·조건부 렌더 — 리듀서/SEED/필터 로직 무변경. esbuild EXIT 0 · node --test 62/62 · 널바이트 0 · 파일 완전성 확인.
- 2026-08-15(야간) **브랜드 일관성 — 쿨 팔레트 잔재 정리(퍼블릭 표면)**: 반려된 쿨 전환(2026-08-12)의 잔재 2곳 발견·수정 — ① index.html `theme-color` #2E6BF0→#BE5535(모바일 주소창 틴트), ② public/favicon.svg fill #2E6BF0→#BE5535(브라우저 탭 아이콘). 인앱 로고(C.brand)·앱 아이콘 PNG는 이미 테라코타라 이 두 곳만 블루로 남아 있었음. 순수 표현·마크업만, src 로직 무변경. esbuild EXIT 0 · 널바이트 0 · 파일 완전성 확인.: 중앙 유틸 `src/eum/validate.js`(표준 결과 포맷 {ok,value|message}, 제어문자 제거·길이 상한: 이의사유/처리메모 500자·공지 제목 80/본문 2000자, 클라이언트 연타 방지 throttleAction — 서버측 rate limit 대체 아님) 신설. 적용 4곳: 청년·어르신 이의신청, 코디 이의 처리 메모, 코디 공지 작성(+발송 연타 방지 2초). 리듀서/SEED 무변경, 실패 메시지에 입력값 미반영(개인정보 원칙). tests/validate.test.mjs 5건 — 62/62 통과 · esbuild EXIT 0 · 파일 완전성 확인.
- 2026-08-14(야간) **코디 콘솔 타이포 하한 정리**: CoordinatorApp의 12.5px 미만 잔글씨 49곳(10~11.5px)을 디자인 시스템 캡션 하한 12.5px로 상향 — 청년(8f43c90)·양육가정(60c4622)과 동일 문법으로 3개 소비자·운영 화면 전부 하한 통일 완료(어르신 앱은 해당 없음). AI 어드바이저·매칭 보드·활동 검증·정산 표·이의 목록·안전 이슈·참여자 검색·공지 전달상태 등 순수 표현만, 리듀서/SEED/코디 로직 무변경. esbuild EXIT 0 · node --test 57/57 · 2479줄 파일 완전성 확인.
- 2026-08-13(야간) **안전 루프 보완 — 어르신 SOS 접수·처리 상태 표시**: SeniorSosCard가 내(reported_by=본인) 안전 이슈를 읽어, 미처리(open)면 '접수됨·확인 중' 배너, 처리(resolved)면 처리일+조치 내용 배너를 카드에 상시 표시(role=status, 큰 활자). 토스트가 사라진 뒤·재방문 시에도 전달 여부를 화면으로 보증 — 정산 이의 카드와 동일 문법. 순수 표현·additive(state prop 옵션, 리듀서/SEED 무변경). esbuild EXIT 0 · node --test 57/57.
- 2026-08-03 **#1 정산 이의신청 처리** — 참여자 이의 등록(RAISE_SETTLEMENT_DISPUTE) → 코디 승인·반려+메모(RESOLVE_SETTLEMENT_DISPUTE).
- 2026-08-03 **#2 채널별 공지 발송 + 전달 결과** — 채널 선택·수신자별 전달상태·미전달 재발송(SEND_NOTICE / RESEND_UNDELIVERED, 시뮬레이션).
- 2026-08-05 **#3 실적 월/분기 집계·CSV 다운로드 + 참여자 통합 검색**.
- 2026-08-13 **런치 P0 — 접근·감사 로그(additive)**: dispatch 훅에서 감사 대상 액션(로그인/로그아웃·정산 이의·공지 발송/재발송·안전 이슈·매칭·검증 등 16종)을 `src/eum/audit.js` 링버퍼(300건)에 기록. 코디 콘솔 '감사 로그' 화면(분류 필터·CSV 다운로드, `src/eum/apps/AuditLog.jsx`) 추가. 개인정보 원칙: 자유 텍스트(이의 사유·공지 본문·처리 메모) 미기록, 식별자·상태값만. 세션 메모리 한정 — 서버 영구 보관·원격 전송은 [승인 필요]. tests/audit.test.mjs 7건(57/57).
- 2026-08-10 **#2 보완 — 참여자 공지 수신함(받는 쪽 완성)**: 청년·어르신·학부모 앱에 '공지' 탭과 수신함 추가. 본인에게 전달(delivered)된 공지만 최신순 노출, 펼치면 읽음 기록(MARK_NOTICE_READ → notices[].read_by), 알림 벨에 미읽음 건수. 코디 공지 화면에는 '읽음 N' 배지·수신자별 읽음 표시. 순수 유틸 src/eum/notices.js + tests/notices.test.mjs(5건).

→ 백로그 3건 소진. 이후 회차는 저위험 UX·접근성·디자인 개선 또는 위 기능의 운영 보완(additive)으로 진행.

## 반영하지 말 것 (가드레일)
- 🎨 **팔레트 확정: 웜 테라코타 #BE5535 + 크림 배경** (`src/eum/theme.js` = 단일 기준, 확정 시안 `9. 디자인개선/이음_디자인시안.html`).
  2026-08-12에 시도한 쿨 전환(딥네이비 #0E1A30 + 로열블루 #2E6BF0)은 **사용자가 반려**했고 2026-08-13에 웜으로 복귀 완료.
  쿨 팔레트로 되돌리지 말 것. 다크 서피스는 웜 에스프레소(#241D17·#332A22·#43382D), 다크 위 보조 텍스트 #B6A89C.
  색은 하드코딩하지 말고 `C.*` 토큰을 쓸 것.
- **가상 KPI 수치 금지**(30%·90%·4.5 등) — 계획서 근거로 쓸 수 없음. 화면에 임의 성과지표 넣지 말 것.
- PRD 원문(배경·문제·해결·차별점) 재서술 불필요 — 이음 자체 문장이 더 정확.
- 다중 안전검증·기술근거·BM·로드맵·대표 경력은 이미 이음 계획이 더 강함 → 건드리지 말 것.

## 개발 규칙
- 회당 1건, additive만. 기존 SEED/리듀서/코디 로직 보존.
- 검증: esbuild EXIT 0(EumApp.jsx 번들). 파일 완전성 host Read 확인. 편집은 bash+python(널바이트 주의).
- 개인정보 실수집·RLS·결제 활성화는 건드리지 말 것(별도 [승인] 트랙).
