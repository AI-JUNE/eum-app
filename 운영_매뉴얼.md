# 이음(Eum) 운영 매뉴얼 (RUNBOOK)

> 대상: 운영자·PM(김건엽) · 스택: Vite + React 19(SPA, 정적 배포) · 운영: 주식회사 고원(GOWON)
> 성격: 상용 런치 준비 문서. **모든 상용 스위치는 기본 OFF**이며 활성화에는 **[승인 필요]**.
> 최종 갱신: 2026-07-28 (자동 하드닝 회차)

---

## 0. 한눈에 보기 (상용 게이트 상태)

| 영역 | 마스터 스위치(env) | 기본값 | 활성화 조건 |
|---|---|---|---|
| 구독결제(PortOne/Toss) | `VITE_BILLING_ENABLED` | `false` (OFF) | [승인 필요] · 서버 결제검증·웹훅 신설 후 |
| 원격 텔레메트리 전송 | `VITE_TELEMETRY_ENABLED` | `false` (OFF) | [승인 필요] · 수집서버·보관정책·법무검토 후 |
| 법적고지 게시 | `LEGAL_META.status` | `draft` | [승인 필요] · 법무 검토 후 `published` |
| AI 호출(callClaude) | (서버 프록시 미구현) | 비활성 | [승인 필요] · 서버 프록시 신설 후 (P1) |

> 원칙("build now, activate on approval"): 결제·개인정보 수집·실발신·실키 연동은 **코드/스캐폴딩까지만** 완료돼 있고 플래그 OFF다. 실제 켜기는 절대 자동으로 하지 않는다.

---

## 1. 빌드 · 배포

```bash
# 개발 서버
npm run dev

# 프로덕션 빌드 (결과물: dist/)
npm run build

# 빌드 미리보기
npm run preview

# 회귀 테스트 (의존성 없음)
npm test          # = node --test tests/*.test.mjs
```

- 산출물은 정적 파일(`dist/`)이라 정적 호스팅(Vercel/Netlify/Cloudflare Pages 등)에 그대로 배포한다.
- 자동 배포: 저장소 루트의 `autopush.ps1`(Windows AutoPush)이 정본 변경을 감지해 커밋·푸시한다. **운영자/에이전트는 코드 커밋까지만** 하고 push·배포는 AutoPush 또는 `auto-deploy.ps1`에 맡긴다.
- 버전 표기: 배포 시 `VITE_APP_VERSION`을 주입하면 텔레메트리 로그에 앱 버전이 남는다(미주입 시 `dev`).

---

## 2. 환경변수 (.env)

상용 전까지는 아래를 **설정하지 않거나 명시적으로 false**로 둔다.

```dotenv
# 결제 (승인 전 절대 true 금지)
VITE_BILLING_ENABLED=false
VITE_PAYMENT_PROVIDER=portone            # portone | toss
VITE_PORTONE_STORE_ID=test_imp_store     # 승인 후 발급값으로 교체
VITE_PORTONE_CHANNEL_KEY=test_channel_key
VITE_TOSS_CLIENT_KEY=test_ck_placeholder # 토스 테스트키(test_ck_...)

# 텔레메트리 (승인 전 절대 true 금지)
VITE_TELEMETRY_ENABLED=false
VITE_TELEMETRY_ENDPOINT=                 # 자사 프록시 URL(승인 후)

# 버전
VITE_APP_VERSION=                        # 예: 2026.07.28
```

> **실키 하드코딩 금지.** 소스에는 placeholder만 두고 실제 키는 배포 환경변수로만 주입한다.

---

## 3. 에러 모니터링 · 로깅 (telemetry.js)

- 부팅 시 `main.jsx`가 `installGlobalHandlers()`를 1회 호출 → `window.onerror`·`unhandledrejection`을 텔레메트리로 연결.
- React 렌더 오류는 `EumErrorBoundary`(EumApp.jsx)가 잡아 `captureError`로 기록.
- 최근 이벤트는 **로컬 링버퍼(최대 100건)**에 보관. CS/디버그 시 브라우저 콘솔에서 스냅샷 확인:
  ```js
  // 개발자도구 콘솔
  import('/src/eum/telemetry.js').then(t => console.table(t.getRecentLogs()));
  ```
- **원격 전송은 기본 OFF**(`TELEMETRY_ENABLED=false`, 엔드포인트 미설정 시 no-op). 개발 모드에서는 콘솔에만 기록.
- **개인정보 원칙**: 이름·연락처 등 PII를 전송하지 않는다. 전송 대상은 오류메시지/스택/경로/타임스탬프/앱버전 등 기술 메타로 한정. 스택은 4000자에서 절단.
- 회귀 가드: `tests/telemetry.test.mjs`(12건) — 링버퍼 상한·최신순 스냅샷·직렬화·"전송 비활성" 가드레일 검증.

### [승인 필요] 원격 전송 활성화 절차
1) `VITE_TELEMETRY_ENABLED=true` · `VITE_TELEMETRY_ENDPOINT=https://<자사프록시>/ingest`
2) 서버측 수집 엔드포인트·보관정책·개인정보 최소수집 **법무 검토**
3) 배포 후 콘솔/수집서버에서 유입 확인

---

## 4. 구독결제 (billing.js)

- 요금제 3단계(정본 = `PLANS`): `무료(0)` · `안심 베이직(19,900/월)` · `안심 프리미엄(39,900/월)`.
- 화면: `ConsumerPricing`(요금제 랜딩). 버튼은 `BILLING_ENABLED=false`면 "구독 신청(준비중)"으로 표시되고 실결제 호출 없이 안내만 반환.
- 결제사: PortOne(아임포트) 또는 Toss. SDK URL·키는 `PROVIDER_CONFIG`에 placeholder로 존재.
- 회귀 가드: `tests/billing.test.mjs`(11건) — "승인 전 결제 비활성" 가드레일 포함.

### [승인 필요] 결제 활성화 절차
1) `VITE_BILLING_ENABLED=true` · `VITE_PAYMENT_PROVIDER=portone|toss`
2) 각 provider **테스트키**로 검수 → 통과 후 라이브키 교체
3) **서버측 결제검증·웹훅 엔드포인트 신설**(클라이언트 단독 결제 금지)
4) 결제/환불 실집행은 반드시 사람이 수행

---

## 5. 법적 고지 (legal.js)

- 이용약관(`TERMS_SECTIONS`)·개인정보처리방침(`PRIVACY_SECTIONS`) 초안이 앱 내 `LegalModal`로 노출.
- `LEGAL_META.status='draft'` — **정식 게시 전 법무 검토 필수**. 회사정보·연락처·보관기간을 사업자등록증/실제 정책에 맞춰 확정.
- [승인 필요]: 검토 완료 후에만 `status`를 `published`로 변경.

---

## 6. AI 호출 (api.js) — P1 [승인 필요]

- `callClaude`가 현재 브라우저에서 `api.anthropic.com`을 직접 호출 → **운영에서 401**(인증 헤더 없음), 그리고 **키를 클라이언트에 노출하면 안 됨**.
- 상용화 전 **서버(프록시) 엔드포인트 신설** 후 fetch 대상 URL 교체 필요. 그 전까지 AI 추천/리포트 요약 기능은 비활성 취급.

---

## 7. 장애 대응 (Incident)

1. **증상 파악**: 사용자 보고 + 브라우저 콘솔 + `getRecentLogs()` 스냅샷.
2. **범위 판단**: 특정 화면만인지 전체인지. `EumErrorBoundary` 폴백 화면이 뜨면 렌더 크래시.
3. **롤백 우선**: 최근 배포가 원인이면 이전 정적 빌드로 롤백(호스팅 대시보드) 후 원인 분석.
4. **데이터**: 현재 파일럿은 클라이언트 상태(`storage.js`, 브라우저 저장소) 기반이라 서버 DB 파괴 위험은 없음. 상태 스키마 변경 시 `normalizeState` 마이그레이션 경로 확인.
5. **커뮤니케이션**: 결제/개인정보 관련 이상 징후는 즉시 운영 책임자에게 에스컬레이션(자동 처리 금지).

---

## 7-1. 배포 헬스체크 (/health.json)

빌드 시 `vite.config.js`의 `eum-health-json` 플러그인이 산출물 루트에 `health.json`을 생성한다.
배포 직후 **`https://<도메인>/health.json`** 을 열어 "어떤 빌드가 떠 있고 어떤 게이트가 켜져 있는지"를 1회 요청으로 확인한다.

| 필드 | 의미 | 확인 포인트 |
|---|---|---|
| `version` / `commit` | `VITE_APP_VERSION` / `VITE_COMMIT_SHA` | 방금 올린 빌드가 맞는지 |
| `builtAt` | 빌드 시각(UTC) | 캐시된 구버전이 남아있지 않은지 |
| `mode` | vite 모드 | 운영은 `production` |
| `flags.auth/billing/telemetry` | 상용 게이트 | 승인 전에는 **전부 false** 여야 함 |
| `configured.*` | 키·엔드포인트 **주입 여부(boolean)** | 값은 절대 노출하지 않음 |
| `paymentProvider` | `portone` \| `toss` | 오설정 진단용(시크릿 아님) |
| `legal.status` | `draft` \| `published` | 정식 게시 승인 전에는 `draft` |

보안 규칙: 시크릿·URL·개인정보는 값이 아니라 **존재 여부만** 담는다. 화이트리스트 방식이라 알 수 없는 `VITE_*`는 덤프되지 않으며,
시크릿처럼 보이는 문자열이 섞이면 `findSecretLeaks`가 감지해 **빌드를 실패**시킨다. 회귀 가드는 `tests/health.test.mjs`.

운영 점검: 배포 후 `flags`가 예상과 다르면 즉시 롤백하고 환경변수를 확인한다(승인 없이 게이트가 켜진 것은 사고로 간주).

---

## 8. 릴리스 전 체크리스트

- [ ] `npm run build` 성공(dist 생성)
- [ ] `npm test` 전건 통과(billing·telemetry 가드 포함)
- [ ] 상용 스위치 4종 상태 확인(결제/텔레메트리 OFF, 법적고지 draft, AI 프록시 미가동)
- [ ] `.env`에 실키 하드코딩 없음(placeholder만)
- [ ] `VITE_APP_VERSION` 주입
- [ ] 배포 후 `/health.json` 확인 — `commit`이 방금 올린 것과 일치하고 `flags` 3종이 모두 `false`
- [ ] 법적고지 문안 최신본 반영 여부(게시는 법무 승인 후)

---

## 부록. 되돌리기 어려운 변경 (자동 금지 — 사람 승인)

결제/환불 실집행, 실개인정보 수집 개시, 원격 텔레메트리 실전송, 법적고지 정식 게시, 라이브 결제키 주입은 **에이전트가 자동으로 수행하지 않는다.** 모두 [승인 필요] 항목이다.
