# 이음 플랫폼 통합 개발 가이드 — 2R MVP (목표 90점+)

작성 2026.9.5 · 대상: 이음 개발 작업창(Claude) + MVP 제작업체
전제: 지원금 700~1,000만원(외주 전용, 인건비 불가) · 제작 9/23~10/23 · 중간빌드 10/10 · 오디션 10/14 · 활동보고서 10/16
평가: 지역 오디션은 **실제 MVP 구현도**로 판정. 활동보고서 문항1(구현·검증)·문항3(1R 대비 개선)이 근거자료.

---

## 0. 현재 상태 진단 — 4개 서비스 실사 결과

| 서비스 | 확인한 것 | 판정 |
|---|---|---|
| **eum-app** (이음 웹) | Vite React SPA. 페르소나 10명·복지정보 카드·5요소 매칭(근접도·시간적합·관심역량 시너지·안전검증·세대보완)이 **전부 JS 번들 안에 하드코딩**. 백엔드 없음. 역할 5종(청소년·중년서포터·어르신·양육가정·코디네이터). 에러 메시지 체계는 잘 잡혀 있음 | **UI 프로토타입**. 데이터가 코드에 있어 「써보라」고 줄 수 없음 → **2R 외주 본체** |
| **callbot-portal/admin** (AICC 콘솔) | SSR 단일 페이지에 메뉴 40여 개. gemini-2.5-flash + edge-tts 실연동(B149). 음성상담·텍스트상담은 동작. 그러나 STT 96.2%·의도정확도 94.8%·상담사 4명·회선 200·인입 4,182 등 **KPI 전부 가짜 상수**. 「제안서 기준」「화면설계서 기준」 라벨 다수 | **엔진은 살아 있고 콘솔은 목업**. 이음에는 「안부」 시나리오 1개만 필요 |
| **d-ars** (디지털 ARS) | SPA, 서버 렌더 없음. 이전 확인: 타사 로고·「신한은행 도입사례」·요금표 | **허위 표기 제거 전에는 심사 노출 금지** |
| **GPTs** (ChatGPT 챗봇) | OpenAI 플랫폼 종속. 이음 계정·DB와 무관 | 데모 보조용. **이음 내부 챗으로 대체** |

**핵심 문제 하나**: 4개가 계정·데이터·URL이 전부 다르다. 심사위원 입장에서는 「만든 게 4개」가 아니라 **「연결 안 된 게 4개」**로 보인다.

---

## 1. 통합 원칙 — 무엇을 하나로 만드는가

```
                    ┌─────────────────────────────────────┐
                    │         이음 플랫폼 (단일 도메인)         │
                    │   단일 계정 · 단일 DB · 단일 감사로그       │
                    ├──────────┬──────────┬───────────────┤
   접점(채널)        │  웹/모바일  │  전화     │  챗            │
                    │  eum-app  │ 안부콜봇   │ 웹챗(GPTs 대체) │
                    │          │ 디지털ARS  │               │
                    ├──────────┴──────────┴───────────────┤
   코어              │  매칭 엔진 · 활동 인증 · 안부 판정 · 실적 집계 │
                    ├─────────────────────────────────────┤
   데이터            │  Postgres (Supabase) · Storage · Auth   │
                    └─────────────────────────────────────┘
                              ▲
                              │ API (안부 시나리오 1개만)
                    ┌─────────┴───────────┐
                    │ Conversation Core   │  ← callbot-portal 엔진 재사용
                    │ (STT·LLM·TTS 어댑터) │     콘솔 UI는 이음에 안 들어감
                    └─────────────────────┘
```

**원칙 5개**

1. **계정 하나** — 어르신·참여자·담당자·코디네이터가 같은 Auth. 역할은 컬럼.
2. **데이터 하나** — 4개 서비스가 같은 `activities`, `matches`, `checkins` 테이블을 읽고 쓴다. 콜봇이 안부 결과를 쓰면 담당자 현황판에 즉시 뜬다.
3. **AICC 콘솔은 이음에 넣지 않는다** — 그건 별도 사업(AICC 라인). 이음이 쓰는 건 「안부 전화 시나리오」 API 하나. 콘솔의 40개 메뉴는 이음 심사에서 **감점 요인**(관련 없는 기능 과잉 + 가짜 수치).
4. **가짜 수치 0건** — 화면에 보이는 모든 숫자는 DB 쿼리 결과여야 한다. 시드 데이터는 「데모 데이터」 배지를 단다.
5. **실데이터 우선** — 테스트 10명이 실제 계정으로 쓴 기록이 그대로 심사 화면이 된다.

---

## 2. 역할 분담 — 외주 800만원 / 대표 자체

| 영역 | 담당 | 이유 |
|---|---|---|
| **A. 웹 코어 F1~F4** (등록·매칭·인증·현황판) + DB + 배포 | **외주 (팔칠구삼)** | 계획서·과업지시서 범위. 검수 대상 |
| B. 안부 콜봇 → 이음 DB 연동 | 대표 | callbot-portal 엔진 보유. 외주 범위 밖 |
| C. 디지털 ARS 어르신 선택 화면 → 이음 DB | 대표 | d-ars 코드 보유. 정리 + 연동 |
| D. 웹챗 (참여자 FAQ·신청 안내) | 대표 | GPTs 대체. Gemini API + 이음 FAQ 10개 |
| E. 허위 표기 제거 (콘솔·D-ARS) | 대표 | 즉시. 개발 아님 |
| F. 사용자 테스트 설계·수행·결과 보고서 | 대표 | 외주 산출물 아님 |

**B·C·D는 「연동」이지 「신규 개발」이 아니다.** 각각 API 엔드포인트 1~2개 + 화면 1개. 대표가 강원 출장 중 저녁에 할 수 있는 크기여야 하고, **안 되면 빼도 A만으로 90점 방어가 된다**. A가 무너지면 B·C·D가 있어도 0점이다.

---

## 3. 기술 스택 — 결정

| 계층 | 선택 | 근거 |
|---|---|---|
| 프론트 | **기존 Vite React 유지** + React Router + TanStack Query | 새 프레임워크 학습 비용 0. 30일 |
| 백엔드 | **Supabase** (Postgres + Auth + Storage + Row Level Security + Edge Functions) | 서버 코드 최소. RLS로 기관별 데이터 격리가 「설정」으로 끝남. 무료 티어로 MVP 충분 |
| 매칭 엔진 | Postgres 함수 or Edge Function (TypeScript) | 산식이 SQL/TS로 명세와 1:1 대응. 심사 때 「이 함수입니다」 하고 보여줄 수 있음 |
| 추천 사유 문장 | 규칙 템플릿 기본 + (선택) Gemini Flash로 자연문 생성 | 규칙만으로도 됨. LLM은 있으면 좋고 없어도 됨 |
| 콜봇 | 기존 `api/voice.py` + Gemini + edge-tts 유지 | 이미 동작. 이음 쪽은 웹훅 수신 1개 |
| 배포 | Vercel (프론트) + Supabase (백엔드) + **고원 소유 도메인** | `eum.gowon.kr` 같은 도메인 하나. vercel.app 주소는 심사에서 「임시」로 보임 |
| 모노레포 | `apps/web` `apps/senior-ars` `packages/db` `packages/matching` | 4개를 한 저장소로. 소스코드 산출물이 「하나의 플랫폼」임을 증명 |

**하지 말 것**: NestJS·Prisma·Docker·K8s·마이크로서비스. 30일에 맞지 않고 심사위원은 안 본다.

---

## 4. 데이터 모델 — 12개 테이블

```sql
-- 조직·계정
orgs            (id, name, type[복지관|자원봉사센터|행정복지센터], region_code, created_at)
users           (id, org_id, role[admin|staff|participant|senior|guardian], name, phone_enc, email, created_at)
consents        (id, user_id, kind[privacy|activity_pledge|location|photo], agreed_at, version)

-- 참여자·어르신 프로필
participants    (user_id, days[], time_slots[], interests[], dongs[], transport, bio, verified_at)
seniors         (user_id, dong, needs[], preferred_days[], preferred_slots[], risk_flags[], registered_by)

-- 매칭
match_requests  (id, senior_id, org_id, status[open|matched|closed], created_by, created_at)
match_candidates(id, request_id, participant_id, score_total, score_json{proximity,schedule,interest,safety,complement}, reason_text, rank)
matches         (id, request_id, participant_id, decided_by, decision[ai_accept|manual_assign], manual_reason, decided_at, notified_at)

-- 활동·인증
activities      (id, match_id, planned_at, started_at, ended_at, lat, lng, distance_m, status[planned|done|missed|flagged])
activity_photos (id, activity_id, storage_path, taken_at, retain_until)

-- 안부·확인
wellbeing_calls (id, senior_id, channel[callbot|manual], called_at, answered, mood_score, risk_level, transcript_summary, raw_ref)
follow_ups      (id, senior_id, trigger[missed_activity|no_checkin|call_unanswered_2x|risk_high], assigned_to, action, result, closed_at)

-- 감사
audit_logs      (id, actor_id, action, target_table, target_id, before_json, after_json, at)
```

**설계 포인트 3개**
- `match_candidates.score_json`을 **요소별로 저장** → 심사 때 「왜 이 사람인가」를 숫자로 보여줌. 이게 「설명 가능한 AI」의 실체.
- `activity_photos.retain_until` → 규제 스크리닝 대응. 보관기간이 컬럼에 있음.
- `audit_logs` → 담당자가 누구를 왜 배정했는지 남는다. B2G 필수.

**RLS 규칙**: `org_id`가 같은 행만 읽기. 참여자는 자기 행만. 어르신 데이터는 담당자·코디만. 이 세 줄이 「개인정보 격리」 답변.

---

## 5. 매칭 엔진 — 명세 (산출물 6번)

```
score_total = Σ w_i × s_i        (w 기본값: 근접도 .30 / 일정 .25 / 관심 .20 / 안전 .15 / 보완 .10)

s_proximity  = 1.0 같은 행정동 | 0.6 인접동 | 0.2 같은 구 | 0
s_schedule   = |참여자 슬롯 ∩ 어르신 희망 슬롯| / |어르신 희망 슬롯|
s_interest   = |참여자 관심 ∩ 어르신 needs| / |어르신 needs|
s_safety     = 1.0 검증완료+신고0 | 0.5 검증완료+경미 | 0 미검증 → 0이면 후보 제외 (하드 필터)
s_complement = 1.0 세대·역량 보완 규칙 매칭 (예: 디지털코칭 ↔ 스마트폰 도움) | 0.5 부분 | 0
```

**추천 사유 문장 규칙** — 상위 2개 요소만 문장화, 점수 0.5 이상만 언급
> 「우산동 같은 동네에 살고(근접도), 평일 저녁 시간이 겹칩니다(일정). 이전 활동 3회 모두 정상 완료.」

**출력**: 상위 3명. 동점이면 이전 활동 완료 수 우선.
**감사**: 후보 생성 시각·가중치 버전·입력 스냅샷을 `match_candidates`에 남김.

**AI라고 부를 수 있는 근거**: 가중 점수 + 하드 필터 + 설명 생성 = 규칙 기반 추천 시스템. 심사에서 「딥러닝이냐」 물으면 **「설명 가능한 규칙 기반이고, 가중치는 운영 데이터로 조정한다. 블랙박스는 복지 현장에 맞지 않다」**가 정답이다. 과장하지 않는 게 90점이다.

---

## 6. 채널 통합 — 각 서비스가 이음 안에서 되는 것

### 6-1. 안부 콜봇 (callbot-portal 엔진 → 이음)
- 이음이 쓰는 시나리오는 **「안부」 1개**. 콘솔 메뉴 40개는 이음과 무관.
- 흐름: 담당자 현황판 「안부 전화」 버튼 → `POST /api/wellbeing/call {senior_id}` → 기존 `api/voice.py` 호출 (시나리오=안부) → 통화 종료 웹훅 → `wellbeing_calls` insert (answered, mood_score, risk_level, summary) → 현황판 갱신.
- risk_level=high 또는 2회 연속 미응답 → `follow_ups` 자동 생성.
- **실전화 없이도 됨**: 콘솔의 「웹에서 바로 테스트」 경로(전화망 미경유)로 시연. 실회선은 3R.
- 심사 시연: 「어르신 1명 선택 → 안부 전화 → 30초 뒤 현황판에 기분 점수와 요약이 뜬다」.

### 6-2. 디지털 ARS (d-ars → 이음 어르신 선택 화면)
- 용도 한정: **통화 중 문자 링크 → 희망 활동·시간대 선택 → `seniors` 갱신**. 이것뿐.
- 1회용 토큰 링크(5분 만료), 회원가입 없음, 글자 18pt·버튼 4개 이내.
- **로고·도입사례·요금표 전부 삭제**. 페이지 제목은 「이음 어르신 신청」.
- 코드는 `apps/senior-ars`로 모노레포 이관.

### 6-3. 웹챗 (GPTs → 이음 내부)
- GPTs는 OpenAI 계정 필요 + 이음 DB 접근 불가 → 심사용으로 부적합.
- 대체: 참여자 화면 우하단 챗. Gemini Flash + 시스템 프롬프트(이음 FAQ 10개 + 「신청은 이 버튼」 안내). 로그는 `audit_logs`.
- 2일 작업. 안 되면 뺀다. **GPTs 링크는 발표에서 언급하지 않는다.**

### 6-4. 이음 웹 (eum-app → 본체)
- 하드코딩 페르소나 10명 → **시드 스크립트**로 DB 이관, 화면에 「데모 데이터」 배지.
- 테스트 참여자 10명은 실제 가입. 시드와 실데이터가 같은 테이블에 섞이되 `is_demo` 플래그로 구분, 통계에서 제외 가능.

---

## 7. 「상용 수준」 체크리스트 — 90점의 실체

심사위원이 30초 안에 판단하는 것들. **하나라도 빠지면 「데모」로 분류된다.**

| # | 항목 | 기준 | 담당 |
|---|---|---|---|
| 1 | 로그인·역할 | 이메일 로그인, 역할별 화면 분리, 권한 없는 URL 접근 시 403 | 외주 |
| 2 | 개인정보 | 수집 동의 화면, 전화번호 암호화 저장, 화면 마스킹(010-****-1234), 사진 보관기간 컬럼, 관리자 삭제 버튼 | 외주 |
| 3 | 감사로그 | 배정·삭제·내려받기 전부 기록. 관리자 화면에서 조회 | 외주 |
| 4 | 실데이터 | 모든 숫자가 쿼리 결과. 시드는 배지 표시 | 외주+대표 |
| 5 | 에러 처리 | 빈 상태 화면, 로딩, 실패 토스트, 재시도. 흰 화면 0건 | 외주 |
| 6 | 반응형·접근성 | 모바일 375px에서 참여자 전 흐름 완주. 어르신 화면 18pt·대비 4.5:1 | 외주 |
| 7 | 도메인·HTTPS | 고원 소유 도메인. vercel.app 노출 금지 | 대표 |
| 8 | 상태 전이 | 신청→추천→확정→활동→완료 상태가 DB enum. 화면과 일치 | 외주 |
| 9 | 내보내기 | 엑셀 다운로드가 실제 기간 필터 반영 | 외주 |
| 10 | 알림 | 확정 시 이메일 실발송 (Supabase + Resend 무료) | 외주 |
| 11 | 헬스체크 | `/health` 200 + DB 연결 확인. 배포 상태 확인용 | 외주 |
| 12 | 문서 | README·환경변수·시드·배포 절차. 산출물 7종 | 외주 |
| 13 | **가짜 표기 0** | 타사 로고·도입사례·수치·「제안서 기준」 라벨 전부 제거 | 대표 |
| 14 | 데이터 초기화 | 「데모 초기화」 버튼으로 시연 전 상태 복원 | 외주 |
| 15 | 테스트 | 핵심 흐름 E2E 1개(Playwright) + 매칭 함수 단위테스트 5개 | 외주 |

---

## 8. 허위 표기 제거 목록 — 9월 안에

| 위치 | 제거·수정 |
|---|---|
| callbot-portal 전체 | STT 96.2%·의도 94.8%·CSAT 4.4·회선 200·인입 4,182·상담사 4명·리더보드·근태 → **삭제 또는 「샘플 데이터」 워터마크**. 이음 심사에는 이 콘솔 자체를 노출하지 않음 |
| callbot-portal 계정 표 | `admin@callbot` 등 가짜 사용자 → 삭제 |
| callbot-portal 라벨 | 「(제안서 기준)」「(화면설계서 기준)」 → 삭제 |
| d-ars | 타사 로고·「신한은행 도입사례」·요금표 → 삭제. 이음 어르신 신청 화면으로 재구성 |
| eum-app | 「300ms」「99.9%」 류 표기 있으면 삭제 |
| 발표·보고서 | 「AI가 학습한다」 → 「규칙 기반 추천 + 운영 데이터로 가중치 조정」 |

---

## 9. 4주 스프린트

| 주차 | 외주 (A) | 대표 (B~F) | 마일스톤 |
|---|---|---|---|
| **W1** 9/23~29 | 요구사항 확정, 화면정의서, DB 설계서, Supabase 프로젝트·RLS, 시드 스크립트 | 도메인·이메일 발송 세팅, 허위 표기 제거, 테스트 담당자 3명 확정, 수동 배정 기준 시간 측정 | 9/29 설계 승인 |
| **W2** 9/30~10/6 | F1 등록, F2 매칭 엔진+추천 카드+확정, 이메일 통보 | 안부 콜봇 웹훅 → `wellbeing_calls` | 10/6 F1·F2 스테이징 |
| **W3** 10/7~13 | F3 인증(사진·위치), F4 현황판·엑셀, **10/10 중간 빌드** | ARS 선택 화면 연동, 웹챗, IR PPT(10/7), **테스트 10/10~13** | 10/10 시연 가능 |
| **W4** 10/14~23 | 결함 수정, E2E, 산출물 문서 7종 | 오디션 10/14, 활동보고서 10/16, 검수 | 10/23 납품 · 10/28 정산 |

**중간 빌드 정의(10/10)**: F1·F2 완료 + F3·F4 화면 존재(기능 80%) + 시드 데이터 로드 + 도메인 배포. 이 상태로 오디션.

---

## 10. 심사 시연 시나리오 — 90초

```
[0:00] 담당자 로그인 → 현황판: 어르신 12 · 참여자 10 · 이번 주 활동 7 · 확인 대상 2
[0:15] 어르신 「박순자」 클릭 → 「추천 받기」 → 후보 3명 카드 (점수 + 사유 문장)
[0:30] 1위 「김민준 · 같은 우산동 · 평일저녁 겹침 · 활동 3회 정상」 → 「확정」
[0:40] (참여자 폰 화면) 확정 알림 → 「활동 시작」 → 사진 → 「종료」
[1:00] (담당자 화면 새로고침) 활동 1건 완료 반영 · 확인 대상 2→1
[1:10] 「안부 전화」 → 30초 후 기분 점수·요약 표시   ← B 되면
[1:20] 「9월 실적 엑셀」 → 파일 열림
[1:30] 끝. "이 화면을 담당자 3명이 4일간 실제로 썼습니다. 결과는 다음 장."
```

**백업**: 같은 흐름 화면 녹화 90초 mp4. 네트워크 죽으면 재생.

---

## 11. 자체 채점표 — 100점

| 영역 | 배점 | 90점 기준 |
|---|---|---|
| 동작 완성도 | 30 | 시연 시나리오 90초 무중단 완주. 흰 화면·에러 0 |
| 실데이터·검증 | 25 | 테스트 10명 실사용 기록 + 지표 4개(소요시간·수락률·만족도·완료율) |
| 통합도 | 15 | 단일 도메인·단일 계정·단일 DB. 콜봇/ARS/챗 중 1개 이상 실연동 |
| 상용 요건 | 15 | §7 체크 15개 중 13개 이상 |
| 설명 가능성 | 10 | 추천 사유가 요소별 점수로 추적됨. 감사로그 |
| 정직성 | 5 | 가짜 수치·타사 로고 0건. 「규칙 기반」 명시 |

**감점 트리거**: 관련 없는 기능 과잉(AICC 콘솔 노출), vercel.app 주소, 하드코딩 숫자, 「AI 학습」 과장, 시연 중 재로그인.

---

## 12. 다른 작업창에 줄 지시문 (그대로 붙여넣기)

```
목표: 이음 2R MVP. 단일 도메인·단일 계정·단일 DB로 4개 서비스를 통합.
범위: F1 참여자 등록 / F2 5요소 매칭 추천+담당자 확정 / F3 활동 인증 / F4 현황판+엑셀.
스택: 기존 Vite React 유지 + Supabase(Postgres·Auth·Storage·RLS). 모노레포 apps/web, apps/senior-ars, packages/db, packages/matching.
데이터: 위 §4 12개 테이블 그대로. 하드코딩 페르소나는 시드 스크립트로 이관, is_demo 플래그.
매칭: §5 산식·가중치·사유 규칙 그대로. score_json 요소별 저장. 안전 0이면 제외.
연동: callbot 「안부」 시나리오 웹훅 → wellbeing_calls. d-ars는 어르신 선택 화면으로 축소 이관. GPTs는 웹챗으로 대체(선택).
금지: 가짜 수치·타사 로고·「제안서 기준」 라벨·vercel.app 노출·AICC 콘솔 메뉴.
필수: §7 체크리스트 15개. /health. 감사로그. 데모 초기화 버튼.
마일스톤: 10/10 중간빌드(F1·F2 완료, F3·F4 80%), 10/23 납품.
산출물: 배포본·소스·중간빌드·화면정의서·DB설계서·매칭명세·운영매뉴얼.
```
