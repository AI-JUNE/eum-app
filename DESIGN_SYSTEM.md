# 통합 디자인 시스템 v1 — GOWON 제품군 (D-ARS · Callbot · 이음 · PMS)

목표: 4개 앱이 하나의 디자인 언어를 공유해 상용 SaaS 품질과 일관성을 갖춘다.
톤: **토스·카카오 금융형** — 큰 글씨, 넓은 여백, 명확한 위계, 친근하고 신뢰감 있는 UI, 모바일 우선.

이 문서는 "단일 기준(source of truth)"이다. 모든 화면은 여기 정의된 토큰·컴포넌트·패턴을 따른다.

---

## 1. 디자인 원칙 (토스/카카오 금융형)

1. **크고 명확하게** — 본문 16px 이상, 핵심 숫자·금액은 크게(24~40px). 작은 회색 글씨 남발 금지.
2. **여백이 곧 품질** — 요소를 촘촘히 넣지 말고 숨 쉴 공간을 준다. 카드 패딩 넉넉히.
3. **한 화면 = 한 목적** — 화면마다 주요 행동(Primary action) 하나를 분명히. 버튼 위계 명확.
4. **부드럽고 안정적** — 라운드 코너, 소프트한 경계, 과한 그림자·네온·그라데이션 금지(플랫).
5. **모바일 우선** — 터치 타깃 48px 이상, 세로 흐름, 하단 고정 CTA 고려.
6. **상태를 숨기지 않는다** — 로딩·빈 상태·에러·성공을 항상 설계. "아무것도 없음"을 방치하지 않는다.
7. **접근성 기본** — 명도 대비 4.5:1 이상, 포커스 링, aria-label, prefers-reduced-motion 존중.

---

## 2. 색상 토큰

브랜드 색은 앱별로 유지하되(변수로 분리), 구조·상태색은 공통.

```
/* 공통 */
--gray-0:#ffffff  --gray-50:#f7f7f5  --gray-100:#f0efec  --gray-200:#e5e3dd
--gray-300:#d3d1c7  --gray-500:#8a8880  --gray-700:#52514e  --gray-900:#1a1a19
--text:#1a1a19  --text-sub:#52514e  --text-mut:#8a8880
--line:#e5e3dd  --bg:#f7f7f5  --surface:#ffffff

/* 상태색 (금융형: 명확하고 절제) */
--primary:#3182F6      /* 신뢰 블루 (토스톤) — 링크·주요강조 */
--success:#0ca30c  --warning:#f59e0b  --danger:#e5484d  --info:#3182F6

/* 브랜드 (앱별) */
--brand-dars:#be5535   /* GOWON 테라코타 */
--brand-eum:#E15A33    /* 이음 코랄 */
--brand-callbot:#be5535
--brand-pms:#be5535
```

원칙: **주요 행동 버튼 = 브랜드색**, **정보·링크·신뢰 강조 = --primary(블루)**, 상태는 상태색. 다크모드는 각 토큰의 대응값을 매핑(추후 v1.1).

---

## 3. 타이포그래피 (Pretendard)

```
Display   40 / 700  — 큰 숫자·금액·랜딩 헤드
H1        26 / 600
H2        20 / 600
H3        17 / 600
Body      16 / 400  (기본)  line-height 1.6
Body-sm   14 / 400
Caption   13 / 400  (--text-mut)
```
- 폰트: Pretendard. 굵기는 400/600 위주(700은 숫자 강조 한정). ALL CAPS·과한 볼드 금지.
- 숫자·금액은 `font-variant-numeric: tabular-nums`.

---

## 4. 간격·모양

```
간격 스케일: 4 · 8 · 12 · 16 · 24 · 32 · 48 (px)
라운드: 버튼/입력 12, 카드 16, 큰 카드/시트 20, pill 999
경계: 1px solid var(--line)  (그림자는 최소, 기능적 포커스 링만)
카드 패딩: 20~24px
터치 타깃: 최소 48px
```

---

## 5. 핵심 컴포넌트 표준

- **버튼**: Primary(브랜드색 채움, 흰 글씨, 높이 52, 라운드 12, 볼드), Secondary(회색 테두리·흰 배경), Ghost(텍스트만). 한 화면에 Primary는 하나. 로딩 시 스피너+비활성.
- **입력**: 높이 52, 라운드 12, 1px 경계, 포커스 시 --primary 링. 라벨은 위, 힌트/에러는 아래. 에러는 --danger + 아이콘.
- **카드**: 흰 배경, 라운드 16, 패딩 20~24, 경계 1px. 제목(H3)+본문+행동 순.
- **리스트/테이블**: 행 높이 넉넉히(56+), 구분선 옅게, 모바일에선 카드형으로 전환. 정렬·검색 제공.
- **빈 상태**: 아이콘 + 한 줄 안내 + 행동 버튼. 절대 빈 화면 방치 금지.
- **로딩**: 스켈레톤(회색 블록) 우선. 스피너는 보조.
- **토스트/알림**: 하단 또는 상단, aria-live, 3~4초 자동 사라짐, 성공/경고/오류 색 구분.
- **모달/바텀시트**: 모바일은 바텀시트, 데스크톱은 중앙 모달. 배경 딤 0.45.
- **차트**: 색은 의미로만(상태색/브랜드), 축·격자 옅게, 애니메이션 절제, 값 라벨 명확.

---

## 6. 적용 순서 (이음부터)

1. **토큰 적용** — 위 색·타이포·간격을 CSS 변수로 심고, 하드코딩 색/폰트를 변수로 교체.
2. **컴포넌트 표준화** — 버튼·입력·카드부터 통일.
3. **화면별 개선** — 한 화면씩: 위계 정리 → 여백 → 빈상태/로딩/에러 → 반응형 → 마이크로 인터랙션.
4. **검증** — 빌드 통과 + 스크린샷 before/after + 모바일/데스크톱 확인.

주의: 한 번에 한 화면(또는 한 컴포넌트)만. 기능·데이터 로직은 건드리지 않는다.
